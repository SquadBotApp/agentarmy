version: '3.8'

services:
  agentarmy:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - .:/app
    environment:
      - MODELSLAB_API_KEY=${MODELSLAB_API_KEY}
    restart: unless-stopped