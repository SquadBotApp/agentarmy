class Planner:
    def plan(self, input_data):
        """Breaks the problem into tasks, dependencies, durations. Returns input_data if no planning logic."""
        if input_data is None:
            return []
        return input_data
