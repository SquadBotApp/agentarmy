# SpaceOS Agent API Reference

## Endpoints
- `/api/telemetry` — Get current telemetry
- `/api/approve` — Human approval endpoint
- `/api/status` — Agent and mission status

See full OpenAPI spec in future releases.
